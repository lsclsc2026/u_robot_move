# 首版发布检查记录

日期：2026-09-13。测试对象是发布目录的源码副本，原机器人运行工作区没有改写。

## 检查方法

- 对全部发布 Python/Bash/YAML/XML 文件执行语法或解析检查。
- 在从现有容器导出的本地环境副本内，从空 build/install 编译发布源码。
- 测试容器禁用外部网络，未挂载机器人设备，仅共享固定版本 SDK 只读目录和独立发布工作区。
- 运行已有 colcon 测试并记录结果；不发送真实运动请求。
- 压缩视频经 ffprobe 检查：建图 10x 为 32.53 秒 / 3.52 MB，定位 3x 为 44.40 秒 / 2.78 MB，巡逻 10x 为 37.87 秒 / 3.33 MB，均为 H.264 / yuv420p / 30 FPS。

## 已完成结果

- Python 语法解析 34 个文件、Bash 语法检查 8 个文件、YAML 解析 16 个文件、XML 解析 14 个文件通过。
- ROS 2 Humble / x86_64 / GCC 11.4 下完整 colcon 构建成功，包含本项目包与三个 Unitree ROS 消息依赖包。
- `colcon test-result --verbose`：**16 tests, 0 errors, 0 failures, 0 skipped**。其中部分包没有注册测试，因此不能理解为每个模块均有完整自动测试。
- 遥控包构建提示安装保留二进制，原始接收器 C++ 未重编译；第三方头文件存在编译警告。
- 文档相对文件链接、发布文件大小及常见凭据格式扫描通过，原始 bag、build/install/log 和大视频已排除。

实机导航、BLE 链路和实体停车不在本次离线验证范围；适用限制见 [validation.md](validation.md)。

发布审阅发现并修复宿主机遥控入口的容器目录推导和默认目标容器。新增 2 个仅捕获命令参数的回归测试，先复现错误再修复通过；原有 16 项 colcon 测试结果与新增回归测试分别记录。
