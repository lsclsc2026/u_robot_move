slam_toolbox:
  ros__parameters:
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    use_sim_time: false
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan
    mode: mapping
    use_map_saver: true
    # Keep interactive pose-graph manipulation out of normal capture. In this
    # office's repetitive corridors, a false overlap match rewrote map->odom
    # and made the robot appear to cross walls.
    enable_interactive_mode: false
    debug_logging: false

    # Keep the serialized pose graph as the authoritative editable map artifact.
    map_update_interval: 3.0
    resolution: 0.05
    transform_publish_period: 0.05
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    minimum_time_interval: 0.20
    minimum_travel_distance: 0.10
    minimum_travel_heading: 0.10
    throttle_scans: 1
    min_laser_range: 0.55
    max_laser_range: 30.0
    stack_size_to_use: 40000000

    # Scan matching is required to collapse odometry drift into sharp walls.
    # The acceptance/search limits below are deliberately tight so repetitive
    # corridor geometry cannot apply a large false heading correction.
    use_scan_matching: true
    use_scan_barycenter: true
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.40
    link_scan_maximum_distance: 0.75
    loop_search_maximum_distance: 3.0
    # Do not add global loop constraints while capturing the map either.
    # Navigation and patrol later use AMCL for map localization.
    do_loop_closing: false
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45
    correlation_search_space_dimension: 0.3
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.10
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.1745
    coarse_angle_resolution: 0.01745
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
